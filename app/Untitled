"use client"; // Next.js ke client-side click functions ke liye [2]

import React, { useState } from 'react';
import Link from 'next/link';

export default function HomePage() {
  // 1. Simple Slide Data (Dono mein abhi /boy.jpeg hai, aap baad mein image badal sakti hain)
  const slides = [
    {
      image: "/boy.jpeg",
      badge: "✓ Govt Approved Courses",
      title: "We are the Professional School of the Future",
      desc: "Manavta Institute offers high-quality computer education matching modern professional industry standards [2].",
    },
    {
      image: "/boy.jpeg", 
      badge: "✓ Private Certified",
      title: "High-Tech Computer Lab & Smart Classes",
      desc: "Learn programming, digital literacy, and core technologies under the guidance of expert mentors [2].",
    }
  ];

  // Current slide track karne ke liye simple index state
  const [currentIndex, setCurrentIndex] = useState(0);

  // Next slide function
  const nextSlide = () => {
    setCurrentIndex((prev) => (prev === slides.length - 1 ? 0 : prev + 1));
  };

  // Previous slide function
  const prevSlide = () => {
    setCurrentIndex((prev) => (prev === 0 ? slides.length - 1 : prev - 1));
  };

  return (
    <main className="min-h-screen bg-slate-50">
      
      {/* 📸 PURE TAILWIND CAROUSEL BANNER (Full-Width Edge-to-Edge) */}
      <div className="relative w-full h-[400px] md:h-[520px] bg-slate-950 overflow-hidden flex items-center">
        
        {/* Slide Background Image using normal standard img tag to prevent hydration errors */}
        <div className="absolute inset-0 w-full h-full z-0">
          <img 
            src={slides[currentIndex].image} 
            alt="Manavta Banner" 
            className="w-full h-full object-cover opacity-40 transition-opacity duration-500"
          />
          {/* Shadow Overlay taaki text ekdum clear aur crisp dikhe */}
          <div className="absolute inset-0 bg-gradient-to-r from-slate-950 via-slate-900/60 to-transparent"></div>
        </div>

        {/* Slide Content (Left-Aligned Container) */}
        <div className="relative z-10 w-full max-w-[1400px] mx-auto px-6 md:px-16 text-white">
          
          {/* Badge */}
          <span className="inline-block bg-sky-600 border border-sky-400 text-white text-[10px] md:text-xs font-bold px-3 py-1 rounded-md uppercase tracking-wider">
            {slides[currentIndex].badge}
          </span>

          {/* Title */}
          <h1 className="text-3xl md:text-5xl font-black mt-6 max-w-2xl leading-tight text-white drop-shadow">
            {slides[currentIndex].title}
          </h1>

          {/* Description */}
          <p className="text-slate-300 text-sm md:text-base mt-6 max-w-md leading-relaxed drop-shadow">
            {slides[currentIndex].desc}
          </p>

          {/* Action Buttons */}
          <div className="mt-8 flex flex-wrap gap-4">
            <Link 
              href="/courses" 
              className="bg-sky-600 hover:bg-sky-700 text-white font-bold py-3 px-6 rounded-xl text-sm md:text-base transition duration-150 shadow-lg hover:shadow-sky-500/20"
            >
              Explore Courses
            </Link>
            <Link 
              href="/portal" 
              className="bg-white/10 hover:bg-white/20 text-white border border-white/20 font-bold py-3 px-6 rounded-xl text-sm md:text-base backdrop-blur-sm transition duration-150"
            >
              Student Zone
            </Link>
          </div>

        </div>

        {/* Left Arrow Button */}
        <button 
          onClick={prevSlide}
          className="absolute left-4 z-20 bg-black/40 hover:bg-sky-600 text-white w-10 h-10 rounded-full flex items-center justify-center transition duration-150 cursor-pointer text-sm font-bold border border-white/10"
        >
          ❮
        </button>

        {/* Right Arrow Button */}
        <button 
          onClick={nextSlide}
          className="absolute right-4 z-20 bg-black/40 hover:bg-sky-600 text-white w-10 h-10 rounded-full flex items-center justify-center transition duration-150 cursor-pointer text-sm font-bold border border-white/10"
        >
          ❯
        </button>

      </div>

      {/* SECTION 2: Our Popular Programs */}
      <div className="max-w-[1400px] mx-auto px-6 py-16">
        <h2 className="text-2xl md:text-3xl font-black text-center text-slate-800 mb-10">
          Our Popular Programs
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          
          {/* Card 1 */}
          <div className="bg-white p-8 rounded-2xl border border-slate-100 shadow-sm hover:shadow-md transition duration-200">
            <span className="text-3xl">💻</span>
            <h3 className="font-bold text-lg text-slate-800 mt-4">Computer Education</h3>
            <p className="text-slate-500 text-sm mt-2 leading-relaxed">
              Learn modern digital tools, coding basics, and software development with hands-on practice [2].
            </p>
          </div>

          {/* Card 2 */}
          <div className="bg-white p-8 rounded-2xl border border-slate-100 shadow-sm hover:shadow-md transition duration-200">
            <span className="text-3xl">🗣️</span>
            <h3 className="font-bold text-lg text-slate-800 mt-4">Spoken English</h3>
            <p className="text-slate-500 text-sm mt-2 leading-relaxed">
              Build confidence and fluent English communication skills for exams and interviews [2].
            </p>
          </div>

          {/* Card 3 */}
          <div className="bg-white p-8 rounded-2xl border border-slate-100 shadow-sm hover:shadow-md transition duration-200">
            <span className="text-3xl">📚</span>
            <h3 className="font-bold text-lg text-slate-800 mt-4">Competitive Classes</h3>
            <p className="text-slate-500 text-sm mt-2 leading-relaxed">
              Specialized learning modules designed to help you clear competitive academic exams.
            </p>
          </div>

        </div>
      </div>

    </main>
  );
}